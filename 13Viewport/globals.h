#ifndef GLOBALS_H
#define GLOBALS_H

#include <SDL.h>

// The window of the application
extern SDL_Window* gWindow;
// The renderer for the window
extern SDL_Renderer* gRenderer;
// The viewport texture image
extern SDL_Texture* gViewportTexture;

#endif
